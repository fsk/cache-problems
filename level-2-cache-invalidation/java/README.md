# Level 2 — Java

Stale cache: `PUT` DB'yi günceller, cache silinmez.

```bash
cd level-2-cache-invalidation/java
mvn spring-boot:run
```

Main: `com.lab.level2.Level2CacheInvalidationApplication` · SDK **21**
