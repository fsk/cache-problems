server:
  port: 8101

spring:
  application:
    name: level-1-cache-aside
  output:
    ansi:
      enabled: always
  datasource:
    url: jdbc:postgresql://localhost:5433/lab
    username: lab
    password: lab
    hikari:
      maximum-pool-size: 10
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update
    show-sql: false
  data:
    redis:
      host: localhost
      port: 6380
  cache:
    type: redis

level1:
  cache:
    key-prefix: "level1:product"
    ttl-seconds: 30
  db:
    simulated-latency-ms: 150

logging:
  level:
    com.lab.level1: INFO
