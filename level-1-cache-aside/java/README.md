# Level 1 — Java (Spring Boot)

Cache-aside via **`@Cacheable`** / **`@CacheEvict`** ve renkli `[CACHE *]` logları.

```bash
# Önce level-1-cache-aside/docker-compose.yml ile Postgres + Redis
cd level-1-cache-aside/java
mvn spring-boot:run
```

**IntelliJ:** Bu klasörü Maven projesi olarak açın · **SDK 21** · Main: `com.lab.level1.Level1CacheAsideApplication`
